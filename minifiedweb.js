document.addEventListener("DOMContentLoaded",()=>{const tooltip=document.getElementById("tooltip");const dots=document.querySelectorAll(".web-dot");const showTooltip=(e)=>{const rect=e.currentTarget.getBoundingClientRect();tooltip.textContent=e.currentTarget.dataset.tooltip;const x=rect.left+rect.width/2;const y=rect.top-12;tooltip.style.left=x+"px";tooltip.style.top=y+"px";tooltip.classList.add("show")};const hideTooltip=()=>{tooltip.classList.remove("show")};dots.forEach((dot)=>{dot.addEventListener("mouseenter",showTooltip);dot.addEventListener("mouseleave",hideTooltip);dot.addEventListener("focus",showTooltip);dot.addEventListener("blur",hideTooltip)})})
